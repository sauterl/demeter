buildscript {
    ext.kotlin_version = '1.2.30'

    repositories {
        mavenCentral()
    }
    dependencies {
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlin_version"
    }
}

group 'com.github.sauterl'
version '1'

apply plugin: 'java'
apply plugin: 'kotlin'

sourceCompatibility = 1.8

repositories {
    mavenCentral()
    jcenter()
    maven {url 'https://jitpack.io'}
}

dependencies {
    compile "org.jetbrains.kotlin:kotlin-stdlib-jdk8:$kotlin_version"

    compile "com.fasterxml.jackson.module:jackson-module-kotlin:2.9.+"

    // https://mvnrepository.com/artifact/org.twitter4j/twitter4j-core
    compile group: 'org.twitter4j', name: 'twitter4j-core', version: '4.0.6'

    compile group: 'commons-codec', name: 'commons-codec', version: '1.11'

    compile 'com.github.kittinunf.fuel:fuel:1.13.0' //for JVM
    compile 'com.github.kittinunf.fuel:fuel-coroutines:1.13.0' //for Kotlin Coroutines support
    compile 'com.github.kittinunf.fuel:fuel-jackson:1.13.0' //for Jackson support
    compile 'com.github.kittinunf.fuel:fuel-moshi:1.13.0' //for Moshi support
    compile 'com.github.kittinunf.fuel:fuel-forge:1.13.0' //for Forge support

    compile 'com.github.scribejava:scribejava-apis:5.4.0'

    compile 'com.uchuhimo:konf:0.10'

    compile 'org.jsoup:jsoup:1.11.3'

    compile 'org.mapdb:mapdb:3.0.5'

    testCompile group: 'junit', name: 'junit', version: '4.12'
}

compileKotlin {
    kotlinOptions.jvmTarget = "1.8"
}
compileTestKotlin {
    kotlinOptions.jvmTarget = "1.8"
}

jar {
    doFirst {
        from { configurations.compile.collect { it.isDirectory() ? it : zipTree(it) } }
    }
    exclude 'META-INF/*.RSA', 'META-INF/*.SF','META-INF/*.DSA'
    manifest{
        attributes 'Main-Class': 'com.github.sauterl.demeter.MainKt'
    }
}
